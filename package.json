{
  "name": "pohela-boishakh-management",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "install:all": "npm install && npm install --prefix client && npm install --prefix server",
    "db:setup": "node scripts/db-setup.js",
    "build": "npm run install:all && npm run build --prefix client && npm run db:setup",
    "start": "npm start --prefix server",
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\""
  },
  "dependencies": {
    "concurrently": "^8.2.2",
    "dotenv": "^16.4.7",
    "pg": "^8.13.1",
    "bcrypt": "^5.1.1",
    "tailwindcss": "^4.0.0",
    "postcss": "^8.4.0",
    "autoprefixer": "^10.4.0"
  }
}
